targetTemplates:
  UITest:
    platform: iOS
    deploymentTarget: '16.0'
    type: bundle.ui-testing
    settings:
      #TEST_HOST = "$(BUILT_PRODUCTS_DIR)/SmartApp.app/SmartApp"
      #TEST_HOST = "$(BUILT_PRODUCTS_DIR)/Smart.Dev.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/Smart.Dev"
      #BUNDLE_LOADER = "$(TEST_HOST)"
